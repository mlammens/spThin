language: r
sudo: required
warnings_are_errors: true

apt_packages:
  - libhdf5-serial-dev
  - libgdal-dev
  - libproj-dev

r_github_packages:
  - rstudio/rmarkdown
  - hadley/testthat
  - jimhester/covr
  - jimhester/lintr
  
after_success:
  - Rscript -e 'covr::codecov()'
 